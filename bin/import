#!/usr/bin/env node
require('babel/register');

var appPromise = require('../lib/app');
var importer = require('../lib/importer');

appPromise.then(function(app) {
  importer(app.api, function() {
    process.exit();
  });
});
